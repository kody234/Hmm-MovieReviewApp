import 'package:flutter/material.dart';

class ListTrailerScreen extends StatelessWidget {
  const ListTrailerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
